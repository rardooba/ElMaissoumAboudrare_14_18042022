import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

/* 
1. Impossible d'installer le plugin Table react
2. Tutoriel de publication d'un plugin sur npm
3. Gestion des erreurs du formulaire ?
4. Mise en place de Mui

> Mui / Redux toolKit / Redux Persistor


*/